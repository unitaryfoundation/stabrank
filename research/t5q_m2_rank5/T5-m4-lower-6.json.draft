{
  "schema_version": "0.1",
  "orbit": "T5",
  "m": 4,
  "direction": "lower",
  "rank": 6,
  "certificate": {
    "script": "verify_challenge/cert_t5_m2_rank5_attested.py",
    "expect": "CERTIFIED chi(T5^4) >= 6",
    "budget_s": 3600,
    "attested": {
      "batches": "research/t5q_m2_rank5/batch_manifest.json",
      "recomputed": 2,
      "compute_hours": 12.3,
      "hardware": "FILL IN after the run: RunPod CPU pod, 16 vCPU AMD EPYC 4564P on a shared host, fifteen batches in parallel at nice 19; the aggregate's two seeded re-runs on the same pod",
      "note": "The exclusion of every rank-5 decomposition of |T5>^2 lives in the stored batch outputs under research/t5q_m2_rank5/results/ (the 155,422 (pivot, partner) units of the 5-cover enumeration over the 3,900 two-ququint stabilizer states in 74 batches, every 5-set whose span contains |T5>^2 modulo 2013265921 listed by the compiled kernel and re-decided exactly and numerically): 0 sets, 0 undecided. The certificate re-enumerates the dictionary and the units, runs the k = 1..4 censuses, verifies the hashes and geometry of every output, re-decides every stored hit, fails on any undecided unit, and re-runs two batches from scratch; completeness rests on the stored outputs and on research/t5q_m2_rank5/batch.py, which regenerates any batch (batch.py K)."
    }
  },
  "provenance": {
    "author": "this repository",
    "reference": "stabrank research/t5q_m2_rank5",
    "method": "projection monotonicity applied to the attested exact rank-5 exclusion at m=2",
    "date": "2026-09-25",
    "github": [
      "vprusso"
    ],
    "compute": {
      "cpu_hours": 0.0,
      "wall_clock_hours": 0.0,
      "runs": 0,
      "hardware": "none beyond the m=2 certificate, which this cell re-runs"
    }
  },
  "notes": "chi(psi (x) phi) >= chi(psi) when phi has a nonzero computational amplitude, applied 2 times to chi(T5^2) >= 6 (Lean: stabRankP_powVecP_mono in lean_proofs/LeanProofs/Stabilizer/SliceP.lean, for any prime local dimension; every amplitude of |T5> is a fifth root of unity over sqrt 5, so any slice serves). The certificate is the m=2 attested one, which prints this line after certifying the rank-5 exclusion; the cell inherits the attested tier. First lower bound for this cell; with the product witness bounds/T5-m4-upper-64.json the cell is 6 <= chi(|T5>^4) <= 64."
}
