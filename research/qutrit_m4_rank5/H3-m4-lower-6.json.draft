{
  "schema_version": "0.1",
  "orbit": "H3",
  "m": 4,
  "direction": "lower",
  "rank": 6,
  "certificate": {
    "script": "verify_challenge/cert_h3_m4_rank5_attested.py",
    "expect": "CERTIFIED chi(H3^4) >= 6",
    "budget_s": 3600,
    "attested": {
      "batches": "research/qutrit_m4_rank5/batch_manifest_H3.json",
      "recomputed": 2,
      "compute_hours": "PLACEHOLDER: CPU-hours of the full run",
      "hardware": "PLACEHOLDER: pod description, batch counts per host, UTC start and end",
      "note": "The exclusion of every rank-5 decomposition of |H3>^4 lives in the stored batch outputs under research/qutrit_m4_rank5/results/H3/ (PLACEHOLDER counts: stage A batches over the 188,451 full 5-covers of |H3>^2 with distinct independent base states, stage B batches over the 6,112 dependent covers, stage C batches over the 4,888 covers with a repeated state), each matching its covers at the base point x_0 = (1, 1) along qutrits 1, 2 and re-deciding every candidate mod 2013265921 and numerically. The certificate checks the partition against the enumerator, re-enumerates the degenerate covers, verifies the hashes and geometry of every output, re-decides every stored hit, and re-runs two batches from scratch; completeness rests on the stored outputs and on research/qutrit_m4_rank5/batch.py, which regenerates any batch (`batch.py H3 K`)."
    }
  },
  "provenance": {
    "author": "this repository",
    "reference": "stabrank research/qutrit_m4_rank5",
    "method": "two-qutrit all-visible base slice: every rank-5 decomposition of |H3>^4 is carried by a monomial symmetry of |H3> on qutrits 1, 2 to one whose slice at x_0 = (1, 1) has all five terms nonzero (Facts A and B of the note), so that slice is a full 5-cover of |H3>^2; every such cover is enumerated up to the unitary symmetry of |H3>^2 and completed slice by slice under the two-qutrit slice structure lemma, modulo 65521 with exact re-decision",
    "date": "PLACEHOLDER",
    "github": [
      "vprusso"
    ],
    "compute": {
      "cpu_hours": "PLACEHOLDER",
      "wall_clock_hours": "PLACEHOLDER",
      "runs": 1,
      "hardware": "PLACEHOLDER"
    }
  },
  "notes": "With the rank-8 witness bounds/H3-m4-upper-8.json this moves the cell to 6 <= chi(H3^4) <= 8; there is no rank-6 or rank-7 witness. The argument (docs/notes/qutrit_m4_rank5_exclusion.md, section 2) depends on: (1) Fact A, that every one-qutrit slice of a rank-5 decomposition of |H3>^4 has all five terms nonzero: chi(H3^3) = 4 (bounds/H3-m3-lower-4.json), the unique rank-4 decomposition of |H3>^3 (research/constructions/data/H3_m3_rank4.json), and the 0 lifts of case [A] of research/constructions/relaxed_lift.py at R = 5, declared here and not re-run. (2) Fact B of PR #86 (research/constructions/two_qutrit_slice.py, 104 s of tables for H3, not re-run): no two-qutrit slice point of a rank-5 decomposition has exactly three nonzero terms. (3) The base-point argument of the note, section 2: every term is a nine-slice term or a diagonal line term along qutrits 1, 2, at most one term is a line term, every diagonal line meets the orbit {11, 12, 21, 22} of (1, 1) under the swap 1 <-> 2 of |H3> on qutrits 1, 2 and the swap of those qutrits, so one base point per cover suffices. (4) The two-qutrit slice structure lemma of PR #86, which makes the matcher's option sets exhaustive; the matcher allows every flat through x_0 and does not use Facts A and B. (5) Exactness: all amplitudes lie in Q(zeta_12) (|H3> in Q(sqrt 3) after dropping the normalisation), and 65521 and 2013265921 are 1 mod 12, so the modular kernels list supersets of the true covers and matches, and every candidate cover and every hit is re-decided mod 2013265921 and in floating point (research/qutrit_m4_rank5/common.decide_terms), stored hits again by the aggregate. (6) The controls (note, section 5; research/qutrit_m4_rank5/results/H3/control_*.json): the census's full 3-covers and 4-covers of |H3>^2 against the stored rank-3 list and slice_lift.all_decompositions; planted five-term decompositions with generic, diagonal-line, dependent and repeated bases recovered; the rank-4 decomposition of |H3>^3 recovered by 2 + 1 slicing; the rank-8 witness recovered from its all-visible base points with the matcher at rank 8. Everything runs in the Python reference; there is no compiled kernel. Attested rather than reproduced because no certificate budget re-runs the full enumeration."
}
