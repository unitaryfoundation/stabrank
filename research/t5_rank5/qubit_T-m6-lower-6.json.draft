{
  "schema_version": "0.1",
  "orbit": "qubit_T",
  "m": 6,
  "direction": "lower",
  "rank": 6,
  "certificate": {
    "script": "verify_challenge/cert_qubit_t_m5_rank5_attested.py",
    "expect": "CERTIFIED chi(qubit_T^6) >= 6",
    "budget_s": 3600,
    "attested": {
      "batches": "research/t5_rank5/batch_manifest.json",
      "recomputed": 2,
      "compute_hours": "FILL: as the m = 5 bound",
      "hardware": "FILL: as the m = 5 bound",
      "note": "The exclusion of every rank-5 decomposition of |T>^5 lives in the stored batch outputs under research/t5_rank5/results/ (the full 5-covers, 4-covers and 3-covers of |T>^3 up to symmetry, each matched at the base point 00 along qubits 1, 2 with the invisible terms on every flat missing 00 and every candidate re-decided mod 2013265921 and numerically): 0 hits, 0 refused, 0 undecided. The certificate re-enumerates the lists, verifies the hashes and geometry of every output, re-decides every stored hit, fails on any refusal or undecided run, and re-runs two batches from scratch; completeness rests on the stored outputs and on research/t5_rank5/batch.py, which regenerates any batch (batch.py K)."
    }
  },
  "provenance": {
    "author": "this repository",
    "reference": "stabrank research/t5_rank5",
    "method": "projection monotonicity applied to the attested exact rank-5 exclusion at m=5",
    "date": "FILL",
    "github": [
      "vprusso"
    ],
    "compute": {
      "cpu_hours": 0.0,
      "wall_clock_hours": 0.0,
      "runs": 0,
      "hardware": "none beyond the m=5 certificate, which this cell re-runs"
    }
  },
  "notes": "chi(psi (x) phi) >= chi(psi) when phi has a nonzero computational amplitude, applied 1 time to chi(T^5) >= 6 (Lean: stabRankP_powVecP_mono in lean_proofs/LeanProofs/Stabilizer/SliceP.lean). The certificate is the m=5 attested one, which prints this line after certifying the rank-5 exclusion; the cell inherits the attested tier. With bounds/qubit_T-m6-upper-6.json (Lean tier) this settles chi(T^6) = 6, replacing the lower bound of 5 from the projected rank-4 exclusion."
}
