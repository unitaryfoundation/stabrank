build/
.venv/
.venv-*/
.pytest_cache/
__pycache__/

# OS noise
.DS_Store

# Local SA solution scratch (research artifacts not for the repo).
# Tracked paper-supplementary npz files live under paper/solutions/ and
# are committed explicitly.
/solution_*.npz

# LaTeX scratch from invoking pdflatex outside paper/. The paper itself
# has its own .gitignore covering main.* products.
texput.log
.claude/

# Plateau bases saved by autoresearch/loop.py manifests (binary search scratch).
autoresearch/state/plateaus/

# Symmetry-group cache of research/t3_rank7/common.py (rebuilt on demand).
research/t3_rank7/cache/

# Plateau bases saved by the merge-recipe anneals (research/merges).
autoresearch/state/plateaus_merges/
